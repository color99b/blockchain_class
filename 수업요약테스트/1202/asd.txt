초기값 줄때 그냥함수나 변수로주면 리렌더링 할때마다 작업하는데
콜백함수로 주면 처음 렌더링할때만 호출됨
-> 콜백함수, 그냥함수 차이?
 콜백함수 : 다른 함수에 매개변수로 넘겨진 함수
 -> 함수 안에서 실행하는 또 다른 함수 > 함수에의해서 나중에 호출되는 함수

clean up: useEffect에서 setinterval 속 return 에 콜백함수를 주면
unmount 될 때 콜백함수 실행되면서 clearinterval 이 될 수 있음

useRef - ref의 변화는 렌더링 발생 x -> 변수 값 유지
state 변화는 렌더링 -> 컴포넌트 내부 변수들새로운 state로 초기화
state 변화 렌더링 해도 ref 값은 유지

ref -> dom 요소에 접근 용이